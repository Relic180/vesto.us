const defaults = {
    tagName: 'li'
};

export default class BaseRow extends Class.View.Base {
    constructor(options = {}) {
        super(_.defaults(options, defaults));

        // TODO: Build this
    }
}
